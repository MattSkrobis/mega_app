%h1 Books

%table.table.hover
  %thead
    %tr
      %th Title
      %th Author
      %th Description
      %th Genre
      %th Publisher
      %th Edition
      %th Action
  %tbody
    - @books.each do |book|
      %tr
        %td= book.title
        %td= book.author
        %td= book.description
        %td= book.genre
        %td= book.publisher
        %td= book.edition
        %td
          = link_to 'Show', editor_book_path(book), class: 'btn'
          = link_to 'Edit', edit_editor_book_path(book), class: 'btn'
          = link_to 'Destroy', editor_book_path(book), method: :delete, data: {confirm: 'Do you really want to hurt me?'}, class: 'btn btn-danger'
